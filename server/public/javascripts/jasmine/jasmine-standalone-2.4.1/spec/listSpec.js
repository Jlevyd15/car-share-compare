describe("List Page Car Share Service Object Count attribute", function() {
  it("should be zero", function() {
    //expect().toEqual(song);
    expect(srvObj.count).toBe(0);
  });
});

describe("", function() {
	it("", function() {
		
	});
});